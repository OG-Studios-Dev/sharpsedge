# Goosalytics QA Cycle — 2026-03-23

Started from cron at 2026-03-23 14:00 America/Toronto.
Updated after completed Riley / Reagan / Rory / Quinn passes.

## Final verdict
NOT CLEAN, but materially improved during the cycle.

## What passed
- Live site routes checked clean: `/`, `/picks`, `/schedule`, `/props`, `/trends`, `/golf`
- Core APIs returned 200:
  - `/api/picks`
  - `/api/nba/picks`
  - `/api/schedule`
  - `/api/nba/dashboard`
  - `/api/soccer/dashboard`
  - `/api/odds/health`
- Today’s picks generation passes:
  - 3 NHL picks
  - 3 NBA picks
- Sampled live picks met requested quality bar:
  - hitRate >= 60
  - edge > 5
  - reasoning present
- Yesterday history / settlement passes:
  - 2026-03-22 history shows win/loss/push with no pending rows in sampled checks
- Mobile UX pass was clean:
  - no crashes
  - no blank screens
  - no horizontal overflow in Playwright mobile audit
  - league switcher passed on `/schedule`, `/props`, `/trends`, `/picks`

## Fixes completed during this cycle
1. API shape fix (local repo fix)
- `src/lib/pick-history-integrity.ts`
- `sportsbook` now mirrors `book` instead of returning misleading null in history-mapped picks.
- Build passed after the change.

2. Props endpoint fix (local repo fix)
- `src/app/api/props/route.ts`
- `/api/props` now aggregates NHL + NBA + MLB using `Promise.allSettled` and returns source-aware meta.
- Validation after fix showed multi-league output rather than NHL-only output.

3. QA screenshot tooling fix (local repo fix)
- `scripts/qa-screenshots.mjs`
- Invalid route `/leagues/epl` replaced with `/schedule?league=EPL`.
- Re-run passed after the correction.

4. Production ops fix (deployed)
- Commit: `b47bb3d Handle Vercel snapshot fs fallback`
- Fixed prod 500 on `/api/odds/aggregated/snapshot?capture=true&sports=NHL,NBA,MLB`
- Verified post-deploy: endpoint returned 200 and healthy counts.

## Remaining risks / reasons it is NOT HEARTBEAT_OK
1. Props quota / freshness risk
- Local cache files show The Odds API prop quota exhausted on recent days:
  - `data/daily-props-2026-03-21.json` → remaining 0, used 500
  - `data/daily-props-2026-03-22.json` → remaining 0, used 500
- App survives via fallbacks, but props freshness is still at risk.

2. Data-quality honesty flags still present
- Soccer dashboard is live but effectively fallback/empty in sampled checks.
- Golf scrape cache can report fresh/ready while field/matched-player counts are zero.
- NBA dashboard sampled props included `fairOdds: null`.

3. Ops visibility gap
- SportsGameOdds key exists in prod env, but current usage is still not directly surfaced/verified.
- Admin cron observability appears incomplete relative to `vercel.json`.

## Repo / deploy evidence
- Quinn verified and deployed a production fix; prod alias now points at the new deployment after commit `b47bb3d`.
- Homepage cold start can run slightly above target, but warmed responses are acceptable.

## Recommended next actions
1. Deploy the local API fixes if they are not already deployed:
   - `src/lib/pick-history-integrity.ts`
   - `src/app/api/props/route.ts`
   - `scripts/qa-screenshots.mjs`
2. Add surfaced usage/health for SportsGameOdds.
3. Add clearer quota/freshness alerts for The Odds API props path.
4. Tighten honesty copy around empty soccer/golf fallback states.
