# Goosalytics QA Cycle — 2026-03-24

Updated from the 19:00 America/Toronto run.

## Final verdict
NOT CLEAN.

## What passed
- Pages `/`, `/picks`, `/schedule`, `/props`, `/trends`, `/golf` all returned 200.
- Core APIs returned 200:
  - `/api/picks`
  - `/api/nba/picks`
  - `/api/schedule`
  - `/api/nba/dashboard`
  - `/api/soccer/dashboard`
  - `/api/odds/health`
  - `/api/odds/aggregated/snapshot?capture=true&sports=NHL,NBA,MLB`
- Today’s NHL and NBA pick endpoints still produced 3 picks each.
- Sampled picks still met the requested minimum quality bar:
  - hitRate >= 60
  - edge > 5
  - reasoning present
- Snapshot prod fix remains healthy.

## Why this is NOT HEARTBEAT_OK
1. Pick payload integrity is still broken live
- `/api/picks` and `/api/nba/picks` still return `sportsbook: null` while `book` is populated.
- Examples:
  - `OTT Win ML` → `book: Kambi`, `sportsbook: null`
  - `Moussa Diabate Over 2.5 Assists` → `book: Model Line`, `sportsbook: null`
- There is still at least one NBA pick with both `book` and `sportsbook` null:
  - `NY Win ML`

2. Yesterday settlement is still incomplete
- `/api/picks/history?date=2026-03-23&limit=100` returned 6 picks.
- 1 remains pending:
  - `Jalen Suggs Over 4.5 Assists`

3. Soccer dashboard remains fallback-empty
- `/api/soccer/dashboard` meta still shows:
  - `oddsConnected: false`
  - `scheduleCount: 0`
  - `trendCount: 0`

4. `/api/props` looks even thinner in this run
- Live `/api/props` returned only 2 props total.
- Both were NHL-only.
- That is not consistent with a healthy cross-sport props feed.

5. Quota/freshness risk remains unresolved
- No `data/daily-props-2026-03-24.json` was present yet.
- Latest available cache evidence still shows exhaustion on 2026-03-23:
  - `remaining: 0`
  - `used: 500`

6. Cold-start performance is still weak
- Homepage was ~6.6s in this run before other routes responded much faster.

## Bottom line
The app is up, but this is not a clean QA pass.
The remaining blockers are payload integrity, unresolved prior-day settlement, empty soccer fallback state, very thin live props output, unresolved quota/freshness risk, and slow cold-starts.
