# Goosalytics QA Punch-List — Thursday April 2, 2026

> **Context:** Masters week starts April 9. Valero Texas Open starts TODAY (Apr 2–5).
> NHL User-Agent fix (389efbf) just deployed overnight — 3 stuck picks should clear.
> Server-side settle-pending cron live (c839a5b). 15 today's picks (4/1) settle tonight.
> Run this list at 09:00 ET, 14:00 ET, and 20:00 ET. Capture results in the table at bottom.

---

## P0 — Must Pass Before We Call Today Clean

### P0-1 · NHL Settlement — 389efbf Verification
The 3 stuck NHL picks from 3/29–3/31 must resolve on the 07:00 UTC cron run (2 AM ET).

**Test steps:**
1. `GET /api/admin/picks/settle-pending?cron=true` — check `settled` count ≥ 3
2. In Supabase `pick_history`: confirm these three rows now have `result ≠ pending`:
   - DAL 1P ML, gid 2025021172 (3/29) → expected PUSH (P1 score 0-0)
   - STL Over 2.5 Goals, gid 2025021177 (3/30) → expected WIN (STL scored 4)
   - CAR 1P ML, gid 2025021185 (3/31) → expected WIN (CAR 2, CBJ 0 P1)
3. `/picks` page — All-league weekly record should show ≤1 pending from those dates
4. Cross-check: `pick_history` `resolved_at` timestamp is set for all three rows

**Pass:** all 3 rows resolved with correct WIN/PUSH results, `resolved_at` populated.
**Fail:** any still `pending` → investigate NHL API response directly; confirm User-Agent header in fetchJSON reaches NHL boxscore endpoint.

---

### P0-2 · 4/1 Picks Settlement — settle-pending cron first live cycle
15 picks generated yesterday (3 NHL + 5 NBA + 7 MLB) should start settling as 4/1 games finish.

**Test steps:**
1. Morning run (09:00 ET): `GET /api/admin/picks/settle-pending?cron=true` → check response, note `checked` and `settled` counts
2. Afternoon run (14:00 ET): repeat — expect more NBA/NHL picks to resolve post-game
3. Evening run (20:00 ET): expect majority of picks settled
4. Confirm STL Over 2.5 Goals (gid from 4/1 pick) resolves correctly now that User-Agent fix is live
5. MLB hitRate 100% picks (SF Win ML, Ryan O'Hearn, KC Win ML): confirm they resolve and do NOT show inflated edge after the fact

**Pass:** ≥10/15 picks resolved by 20:00 ET with correct WIN/LOSS/PUSH results.
**Fail:** <5 picks resolved by 14:00 ET → check cron logs in Vercel Functions tab.

---

### P0-3 · Production Pick Generation — 4/2 Picks
Daily picks cron fires at 15:00 UTC (11 AM ET). Valero Texas Open starts today — PGA picks should surface.

**Test steps:**
1. **Pre-cron** (before 11 AM): `GET /api/picks` — confirm `source=history_locked` for previous day picks
2. **Post-cron** (after 11:15 AM): `GET /api/picks` — expect new NHL picks
3. `GET /api/nba/picks` — expect NBA picks
4. **Golf/PGA**: `GET /api/golf/picks` (if exists) or check `/golf` page for Valero Texas Open picks surfacing
5. Quality gates — every pick must have:
   - `hitRate >= 65%`
   - `edge >= 10%` (NHL/NBA), `>= 8%` (MLB)
   - `odds > -200`
   - `sportsbook` not null/None
   - `reasoning` > 20 chars
6. Confirm zero picks with `hitRate = 100%` from a sample of < 5 games (flag MLB if present)
7. Total pick count: expect 3–7 per sport; confirm no sport has > 7

**Pass:** All picks pass quality gates, sportsbooks populated, reasoning present.
**Fail:** Any pick below floor, or hitRate=100% from <5-game sample → flag in persistent issues.

---

## P1 — High Priority, Same-Day Fix Required

### P1-1 · Valero Texas Open — Golf Page & PGA Surfacing
First PGA event of Masters week ramp-up. Valero starts Apr 2–5.

**Test steps:**
1. `GET /golf` (browser) → confirm Valero Texas Open appears in tournament list / schedule section
2. Check `golf-api.ts` entry: `id: "valero-texas-open-2026"`, `startDate: "2026-04-02"` — confirm it surfaces as current/active on load
3. `/golf/tournament/[id]` page for valero-texas-open-2026 — confirm it loads without crash, shows leaderboard or pre-tournament data
4. Confirm `isGolfMajor` is FALSE for Valero (not a major), so Masters Analysis section does NOT appear
5. Confirm Masters 2026 (`startDate: "2026-04-09"`) still shows in upcoming section with odds snapshot
6. `GET /api/golf/leaderboard` — confirm Valero returns valid data or graceful empty state
7. `/golf` page cold-start load time: should remain ≤ 1.7s (confirmed stable last 4 days)

**Pass:** Valero surfaces, no crash on tournament page, Masters analysis not injected for Valero, golf page ≤1.7s.
**Fail:** Valero not in list or page crashes → check `getPGASchedule()` filtering logic on `current` flag and date comparison.

---

### P1-2 · Masters 2026 Pre-Tournament Analysis Section
Masters starts Apr 9 — analysis section (25b4e20) and Oddschecker finish-market odds (7c50bd2) must be correct.

**Test steps:**
1. Navigate to `/golf` — confirm `MastersAnalysisSection` renders (not empty, not null)
2. Verify Top 5 / Top 10 / Top 20 finish-market odds are displayed with player names and American-format odds
3. Confirm `getLocalMastersOddsSnapshot()` returns non-null (check: snap has `tournament = "Masters Tournament"`, `startDate = "2026-04-09"`)
4. H2H analyzer: at least 2 head-to-head comparisons shown with DG skill edge data
5. `isGolfMajor` flag: verify Masters returns `true`, Valero returns `false` — check in `golf-ui.ts` / `golf-api.ts`
6. Admin: `GET /api/golf/odds-snapshot?cron=true` — verify it doesn't error for pre-tournament Masters context

**Pass:** Analysis section renders with odds and H2H data; Masters correctly flagged as major.
**Fail:** Empty section or null snapshot → check `getLocalMastersOddsSnapshot()` cold path for pre-tournament data.

---

### P1-3 · Admin Pages — Core Flows
**Test steps:**
1. `/admin` — loads without crash, confirms auth gate works (unauthenticated → redirect to login)
2. `/admin/goose-model` — Signal Lab renders, sport filter pills work, results load
3. `/admin/picks` — pick list loads, shows today's picks with correct status
4. `/admin/ops` — page loads, cron schedule visible (14 crons expected)
5. `/admin/systems` — systems list loads; confirm Fuch's Fade shows `status: tracking` (not `awaiting_data`)
6. `/admin/sandbox` — loads without crash
7. Signal Lab — sport filter pills (NHL/NBA/MLB/PGA) all toggle correctly; "All" shows combined count matching sum

**Pass:** All admin pages load, auth gate enforced, Signal Lab filters work.
**Fail:** Any 500/crash → check Vercel function logs for that route.

---

### P1-4 · Server-Side Settlement Cron — End-to-End Verification
First full day of the new settle-pending cron (c839a5b). Three runs: 07:00, 14:00, 20:00 UTC.

**Test steps:**
1. Vercel dashboard → Functions tab → filter by `/api/admin/picks/settle-pending` → confirm 3 invocations with status 200
2. Each invocation response body: `{"checked": N, "settled": M, "still_pending": K, "results": [...]}`
   - `checked` should be > 0 (picks in 3-day window)
   - `settled` should be > 0 on the 14:00 run (4/1 games finishing)
3. Cross-check: `pick_history` rows from 4/1 have `resolved_at` timestamp from today
4. Verify no double-settlement: same pick should not have multiple conflicting results in `pick_history`

**Pass:** All 3 cron invocations succeed (200), settlement data lands in Supabase.
**Fail:** 400/500 from cron → check route auth (`cron=true` param), Supabase connection, resolvePick imports.

---

### P1-5 · PGA Exclusion from All-League Aggregate Stats
Fix (0a5efe1) should exclude PGA picks from the "All" tab stats on `/picks`.

**Test steps:**
1. Navigate to `/picks` — open "All" sport tab (or default view)
2. Confirm weekly win/loss record does NOT include PGA picks from Houston Open (3/26)
3. Navigate to `/picks/history` — set sport filter to "all" → confirm PGA picks excluded from headline stats (ROI%, record)
4. Switch filter to "PGA" → confirm PGA picks appear in PGA-only lane with tournament-style display
5. Check `/picks/history` subtitle copy — should read "Standard sports exclude PGA by default; PGA lives in its own tournament-style lane"
6. Record check: week-to-date totals (3/23–4/2) should only reflect NHL + NBA + MLB in the "All" view

**Pass:** PGA correctly isolated; "All" stats match non-PGA picks only.
**Fail:** PGA bleeds into All aggregate → re-check `standardHistoryPicks` filter in `/picks/history/page.tsx`.

---

## P2 — Known Issues / Monitoring / Loose Ends

### P2-1 · Soccer Dashboard — Persistent Empty State
`oddsConnected: false`, `scheduleCount: 0` for all of this week. No fix shipped.

**Test steps:**
1. `GET /api/soccer/dashboard` → confirm response; log `oddsConnected`, `scheduleCount`
2. If `oddsConnected: false` still: check upstream soccer odds provider config (env var `SOCCER_ODDS_KEY` or similar)
3. Check if any soccer fixtures should exist for today (Champions League, Premier League, etc.) — confirm the source data isn't simply absent
4. Document: if this is a data provider outage vs code bug, note the difference

**Monitor:** No fix required today, but track — if soccer remains broken by April 7, escalate to P1.

---

### P2-2 · MLB hitRate Calibration — 100% Rates Persist
Multiple MLB picks this week showed hitRate=100% (SF Win ML, Ryan O'Hearn, KC Win ML). This implies 1–2 game sample windows.

**Test steps:**
1. Check today's MLB picks (4/2): flag any with hitRate ≥ 90%
2. For each flagged pick: log the game_id and check how many historical games back the model looked
3. If hitRate 100% is still common in week 2 of MLB season → document for calibration review
4. Target behavior: MLB hitRates should normalize toward 65–78% by end of April as sample grows

**Monitor:** Document count of 100% hitRate MLB picks per day this week.

---

### P2-3 · Homepage Cold-Start Load Time
Consistently 6–8s cold. Vercel cache miss on first hit.

**Test steps:**
1. Hard-refresh `/` (clear cache) → record load time in ms
2. Warm hit (reload immediately) → record load time
3. If cold-start > 10s → check for new server components added to homepage that might block

**Monitor:** No fix required this cycle. Log the cold-start time in the daily record.

---

### P2-4 · Untracked / Uncommitted Files Hygiene
`ingest-production-picks/route.ts` was flagged as untracked on 3/30 afternoon. May have been committed since.

**Test steps:**
1. `git status` in repo root — confirm zero untracked source files in `src/`
2. `git diff --stat HEAD` — no unstaged changes in non-test source files
3. If untracked files found: commit or explicitly `.gitignore` them before next deploy

**Pass:** `git status` shows clean working tree for `src/`.

---

### P2-5 · PGA Unit Calculation — Negative Units Fix Verification
Fix b07b3b9 corrected negative units inflating profit for loss picks.

**Test steps:**
1. `/picks/history` → filter to PGA tab → inspect Houston Open (3/26) PGA picks
2. Confirm: losing PGA picks show negative units (e.g., -1.0), not inflated positive
3. Confirm: winning PGA picks show correct positive units
4. Admin Signal Lab → PGA sport filter → verify unit totals match manual calculation (win count × stake – loss count × stake)

**Pass:** No loss pick shows positive units in PGA history.

---

### P2-6 · NHL Shot Refresh Cron — Timing Check
`/api/admin/nhl-shot-refresh` runs daily at 11:00 UTC (7 AM ET), full refresh Mon at 12:00 UTC.

**Test steps:**
1. Vercel dashboard → confirm `nhl-shot-refresh` cron invocations succeeded last 24h
2. `GET /api/props` → confirm NHL props count is non-zero (expected 20–40 props pre-game)
3. Verify NHL SOG (shots on goal) props are populated for today's slate

**Monitor:** Log NHL props count at 09:00 ET.

---

## Cron Health — Full Schedule Check (14:00 ET Run)

| Cron | Schedule | Last Run Status | Expected |
|------|----------|-----------------|----------|
| `/api/admin/picks/settle-pending` | 0 7,14,20 * * * | ? | 200, settled > 0 |
| `/api/admin/goose-model/generate-daily` | 0 15 * * * | ? | 200, picks stored |
| `/api/admin/goose-model/auto-grade` | 0 7 * * * | ? | 200 |
| `/api/admin/sandbox/auto-grade` | 30 7 * * * | ? | 200 |
| `/api/admin/systems/grade` | 0 9 * * * | ? | 200 |
| `/api/admin/nhl-shot-refresh` | 0 11 * * * | ? | 200 |
| `/api/admin/goose-model/mlb-lineup-refresh` | 0 21 * * * | ? | 200 |
| `/api/systems/refresh` | 0 15 * * * | ? | 200 |
| `/api/odds/aggregated/snapshot` (all) | 17 * * * * | ? | 200 |
| `/api/golf/scrape` | 0 10 Mon–Wed | ? | 200 (Thu = no run) |
| `/api/golf/odds-snapshot` | 0 14,20,2 * * * | ? | 200 |

---

## API Health Baseline — Record at Each Run

| Endpoint | 09:00 | 14:00 | 20:00 |
|----------|-------|-------|-------|
| /api/picks | | | |
| /api/nba/picks | | | |
| /api/schedule | | | |
| /api/nba/dashboard | | | |
| /api/soccer/dashboard | | | |
| /api/odds/health | | | |
| /api/props | | | |
| /api/golf/leaderboard | | | |

---

## Page Health Baseline

| Page | Status | Load Time | Notes |
|------|--------|-----------|-------|
| / (homepage) | | | cold-start target < 10s |
| /picks | | | target < 500ms |
| /schedule | | | target < 500ms |
| /props | | | target < 500ms |
| /trends | | | target < 500ms |
| /golf | | | target ≤ 1.7s |
| /golf/tournament/valero-texas-open-2026 | | | must load without crash |
| /admin | | | must redirect if unauth |

---

## Week Record (carry-forward for 4/2 update)

| Date | Picks | Result |
|------|-------|--------|
| 3/23 | 6 | 3W/2L/1P |
| 3/24 | 6 | 4W/2L |
| 3/25 | 6 | 2W/4L |
| 3/27 | 4 | 3W/1L |
| 3/28 | 6 | 3W/2L/1P |
| 3/29 | 9 | 3W/5L/1P (resolving via 389efbf) |
| 3/30 | 10 | 3W/6L/1P (resolving via 389efbf) |
| 3/31 | 10 | 5W/4L/1P (resolving via 389efbf) |
| 4/1 | 15 | pending — settles today |
| 4/2 | TBD | TBD |

---

## Known Bugs / Open Loose Ends (tracker)

| # | Bug | First Seen | Status | Owner |
|---|-----|-----------|--------|-------|
| B-1 | Soccer oddsConnected=false, scheduleCount=0 | 3/27 | ❌ open, no fix | unknown |
| B-2 | MLB hitRate=100% from tiny samples | 3/29 | ⚠ monitor | model calibration |
| B-3 | Homepage cold-start 6–8s | 3/27 | ⚠ monitor (Vercel cache) | infra |
| B-4 | NHL picks settlement browser-only (root cause) | 3/23 | ✅ fixed c839a5b | shipped |
| B-5 | NHL fetchJSON missing User-Agent (403 from Vercel) | 3/23 | ✅ fixed 389efbf | shipped |
| B-6 | MLB betType mismatch blocking grading | 3/29 | ✅ fixed e3067d6 | shipped |
| B-7 | PGA negative units inflating profit | 3/28 | ✅ fixed b07b3b9 | shipped |
| B-8 | PGA bleeds into All-league stats | 3/30 | ✅ fixed 0a5efe1 | shipped |
| B-9 | Valero Texas Open not surfacing as current event | — | ❓ verify today P1-1 | — |
| B-10 | ingest-production-picks/route.ts untracked | 3/30 | ❓ verify P2-4 | — |

---

## Pass/Fail Summary (fill in at EOD)

| Item | Priority | Result | Notes |
|------|----------|--------|-------|
| P0-1 NHL Settlement (3 stuck picks) | P0 | | |
| P0-2 4/1 Picks Settlement | P0 | | |
| P0-3 4/2 Pick Generation | P0 | | |
| P1-1 Valero Texas Open surfacing | P1 | | |
| P1-2 Masters Analysis Section | P1 | | |
| P1-3 Admin Pages | P1 | | |
| P1-4 settle-pending cron E2E | P1 | | |
| P1-5 PGA excluded from All-league stats | P1 | | |
| P2-1 Soccer dashboard | P2 | | |
| P2-2 MLB hitRate monitoring | P2 | | |
| P2-3 Homepage cold-start | P2 | | |
| P2-4 Untracked files | P2 | | |
| P2-5 PGA units fix verification | P2 | | |
| P2-6 NHL shot refresh cron | P2 | | |
