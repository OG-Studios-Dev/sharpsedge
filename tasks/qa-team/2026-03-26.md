# Goosalytics QA Cycle — 2026-03-26

Started from cron at 2026-03-26 09:00 America/Toronto.

## Final verdict
NOT CLEAN — but one genuine improvement landed.

## What passed
- Pages `/`, `/picks`, `/schedule`, `/props`, `/trends`, `/golf` all returned 200.
- Core APIs returned 200:
  - `/api/picks`
  - `/api/nba/picks`
  - `/api/schedule`
  - `/api/nba/dashboard`
  - `/api/soccer/dashboard`
  - `/api/odds/health`
  - `/api/odds/aggregated/snapshot`
- Today's NHL and NBA pick endpoints produced 3 picks each.
- Sampled picks met the minimum quality bar:
  - hitRate >= 60 (lowest observed: 66.7 on Jalen Brunson)
  - edge > 5
  - reasoning present
- Snapshot endpoint remains healthy.

## Genuine improvement since yesterday
- `/api/props` is now properly multi-league with structured `{props, meta}` response.
  - 36 total props: NHL 2, NBA 34, MLB 0
  - Prior state was 17 NHL-only props with no metadata
  - /props page loaded without error flags — frontend handles the new format

## Why this is NOT HEARTBEAT_OK
1. Pick payload integrity is still broken live
- `/api/picks` and `/api/nba/picks` still return `sportsbook: null` while `book` is populated.
- Root cause identified: the live picks GET route reads `sportsbook` directly from the DB
  column, which is null on engine-generated picks. The recent fix only corrected manual
  repair writes — the engine's own pick writes never populate the sportsbook column.
  - `pick-history-store.ts` line 84: `const resolvedSportsbook = pick.sportsbook || pick.book || null;`
    correctly resolves for history, but the live picks GET path does not apply this logic.

2. Multi-day resolution debt is still accumulating — NHL resolution appears broken
- 2026-03-23: `Jalen Suggs Over 4.5 Assists` still pending (4 days old)
- 2026-03-24: `STL Over 2.5 Goals` + `CAR Over 3.5 Goals` still pending (2 days old)
- 2026-03-25: `Josh Doan Over 2.5 Shots on Goal`, `BUF Win ML`, `Viktor Arvidsson Over 0.5 Points`
  still pending (games were last night — should be resolved by 9 AM today)
- Pattern: NBA picks resolve; NHL picks do not. NHL auto-resolution appears broken.

3. Soccer dashboard remains fallback-empty
- `oddsConnected: false`, `scheduleCount: 0`, `trendCount: 0`

4. Cold-start performance degraded notably this run
- Homepage: 9.4s
- Golf page: 12.4s
- These are the worst observed this week.

5. NBA props statsSource is null on most NBA rows in /api/props
- Minor but affects data-layer observability.

## Summary of unresolved pick settlement across days
| Date       | Total | Pending | Pending picks |
|------------|-------|---------|---------------|
| 2026-03-23 | 6     | 1       | Jalen Suggs Over 4.5 Assists |
| 2026-03-24 | 6     | 2       | STL Over 2.5 Goals, CAR Over 3.5 Goals |
| 2026-03-25 | 6     | 3       | Josh Doan Over 2.5 SOG, BUF Win ML, Viktor Arvidsson Over 0.5 Pts |

NHL auto-resolution appears to not be running — all pending items span multiple days and are NHL.

## Bottom line
One real fix landed (props multi-league). Everything else is unchanged.
The two highest-priority issues remain: (1) sportsbook null on all live picks, (2) NHL resolution completely broken across 3 days.
