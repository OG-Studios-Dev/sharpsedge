# Goosalytics QA Cycle — 2026-03-25

Updated from the 19:00 America/Toronto run.

## Final verdict
NOT CLEAN.

## What passed
- Pages `/`, `/picks`, `/schedule`, `/props`, `/trends`, `/golf` all returned 200.
- Core APIs returned 200:
  - `/api/picks`
  - `/api/nba/picks`
  - `/api/schedule`
  - `/api/nba/dashboard`
  - `/api/soccer/dashboard`
  - `/api/odds/health`
- Today's pick slate is generated: 3 NHL + 3 NBA picks, all with hitRate ≥ 70, edge ≥ 17, reasoning present.
- Today's picks are legitimately pending at 7 PM — games are live/upcoming.
- Snapshot endpoint remains healthy.

## Why this is NOT HEARTBEAT_OK
1. Pick payload integrity is still broken live
- `/api/picks` and `/api/nba/picks` still return `sportsbook: null` while `book` is populated.
- Examples:
  - `BUF Win ML` → `book: Kambi`, `sportsbook: null`
  - `OKC Win ML` → `book: Bovada`, `sportsbook: null`
- This defect has been present since 2026-03-23 and has not cleared despite commits
  targeting it.

2. 2026-03-24 settlement is still incomplete
- `STL Over 2.5 Goals` and `CAR Over 3.5 Goals` are still pending.
- Those NHL games finished last night; resolution should have run.

3. 2026-03-23 settlement still has one lingering pending pick
- `Jalen Suggs Over 4.5 Assists` remains pending 3 days later.

4. Soccer dashboard remains fallback-empty
- `oddsConnected: false`, `scheduleCount: 0`, `trendCount: 0`.

5. `/api/props` is still NHL-only
- 17 props returned, all NHL.
- `/api/nba/dashboard` had 20 NBA props live simultaneously.

6. No daily-props cache files for 2026-03-24 or 2026-03-25
- Quota/freshness state for those days is unverifiable.

## Summary of unresolved pick settlement across days
| Date       | Total | Pending | Pending picks |
|------------|-------|---------|---------------|
| 2026-03-23 | 6     | 1       | Jalen Suggs Over 4.5 Assists |
| 2026-03-24 | 6     | 2       | STL Over 2.5 Goals, CAR Over 3.5 Goals |
| 2026-03-25 | 6     | 6       | All (games in progress — expected) |

## Bottom line
App is up. Today's picks are clean in quality. But sportsbook-field nulls are still live,
multi-day resolution debt is still accumulating, soccer is still empty, and props feed
is still incomplete. Not HEARTBEAT_OK.
